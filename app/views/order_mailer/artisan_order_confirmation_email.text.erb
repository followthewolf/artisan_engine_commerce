You have received an order from <%= @order.patron.first_name + " " + @order.patron.last_name %>!
		
Goods ordered:
		
<% for line_item in @order.line_items %>
	<%= line_item.quantity.to_s + "x " + line_item.name %>
	<%= line_item.options unless line_item.options.empty? %>
<% end %>
		
You can view and manage this order's details at <%= manage_order_url( @order, :host => ArtisanEngine::Commerce.domain ) %>

Love,
ArtisanEngine